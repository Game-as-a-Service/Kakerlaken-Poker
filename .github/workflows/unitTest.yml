name: Unit Test

on: [workflow_call, workflow_dispatch]

jobs:
  unit_test:
    runs-on: ubuntu-latest
    steps:
      - name: ⬇️ Checkout
        uses: actions/checkout@v4

      - name: Unit Test
        run: pnpm test
