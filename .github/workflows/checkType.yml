name: Check Type

on: [workflow_call, workflow_dispatch]

jobs:
  checkout:
    uses: ./.github/workflows/checkout.yml
    secrets: inherit

  check_lint:
    needs: checkout
    runs-on: ubuntu-latest
    steps:
      - name: Check Type
        run: pnpm typecheck
