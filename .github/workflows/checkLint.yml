name: Check Lint

on: [workflow_call, workflow_dispatch]

jobs:
  check_lint:
    runs-on: ubuntu-latest
    steps:
      - name: ⬇️ Checkout
        uses: actions/checkout@v4

      - name: Check Lint
        run: pnpm lint
